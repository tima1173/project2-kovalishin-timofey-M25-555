# PrimitiveDB — Простая файловая база данных

PrimitiveDB — это мини-СУБД с интерфейсом командной строки, написанная на Python.  
Она позволяет управлять таблицами, храня всю структуру в одном JSON-файле (`db_meta.json`).  
Подходит для обучения, прототипирования и простых задач хранения данных.

---

## 🚀 Возможности

- Создание таблиц с указанием столбцов и типов;
- Просмотр списка таблиц;
- Удаление таблиц;
- Автоматическое добавление столбца `ID:int`;
- Поддержка типов: `int`, `str`, `bool`;
- Хранение метаданных в `db_meta.json`.

---

## ✅ Доступные команды

### 🟢 `create_table <имя_таблицы> <столбец1:тип> [столбец2:тип ...]`
Создаёт новую таблицу с указанными столбцами.

- **`<имя_таблицы>`** — имя таблицы (без пробелов).
- **`<столбец:тип>`** — имя столбца и тип, разделённые двоеточием.
- Автоматически добавляется столбец `ID:int`.
- Поддерживаемые типы: `int`, `str`, `bool`.

#### 💡 Пример:
```bash
create_table users name:str age:int active:bool
→ Создаёт таблицу users со столбцами:
ID, name, age, active.

⚠️ Возможные ошибки:
name:string → ❌ неподдерживаемый тип (должно быть str);
name → ❌ отсутствует :тип;
Таблица уже существует → выводится сообщение об ошибке.
🟢 list_tables
Показывает список всех созданных таблиц.

💡 Пример:
bash
list_tables
🔹 Результат:
Список таблиц:
  - users
  - products
Если таблиц нет:

Таблицы отсутствуют.
🟢 drop_table <имя_таблицы>
Удаляет таблицу по имени.

💡 Пример:
bash
drop_table users
→ Удаляет таблицу users.

⚠️ Если таблицы нет:
Ошибка: таблица 'users' не найдена.
🟢 help
Показывает справочную информацию о всех командах.

💡 Пример:
bash
help
🔹 Вывод:

***Процесс работы с таблицей***
Функции:
<command> create_table <имя_таблицы> <столбец1:тип> .. - создать таблицу
<command> list_tables - показать список всех таблиц
<command> drop_table <имя_таблицы> - удалить таблицу

Общие команды:
<command> exit - выход из программы
<command> help - справочная информация

🟢 exit
Завершает работу программы.

💡 Пример:
bash
exit
🔹 Вывод:
Программа завершена.